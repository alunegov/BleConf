pluginManagement {
    repositories {
        google()
        gradlePluginPortal()
        mavenCentral()
    }
    
}

rootProject.name = "untitled"

include(":androidApp")

enableFeaturePreview("VERSION_CATALOGS")
